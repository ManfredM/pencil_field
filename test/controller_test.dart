import 'package:flutter_test/flutter_test.dart';

/// The controller is mainly tested as part of PencilField. This test only
/// covers some routines that are not tested together with the widget.

/// The empty main is needed to run flutter test
void main() {
  group('Empty (all tests done in field_test.dart', () {
    test('Empty', () {});
  });
}
